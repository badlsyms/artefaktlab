const chat=document.querySelector('#chat');const form=document.querySelector('#composer');const input=document.querySelector('#message');const send=document.querySelector('#send');const statusEl=document.querySelector('#status');const pin=document.querySelector('#pin');const clear=document.querySelector('#clear');const diagnostics=document.querySelector('#diagnostics');const STORE='nucleus-history-v2';const PIN_STORE='nucleus-pin-v2';let history=[];try{history=JSON.parse(localStorage.getItem(STORE)||'[]')}catch{history=[]}pin.value=localStorage.getItem(PIN_STORE)||'';function addMessage(role,text,persist=true){const wrap=document.createElement('article');wrap.className=`message ${role==='user'?'user':'assistant'}`;const avatar=document.createElement('div');avatar.className='avatar';avatar.textContent=role==='user'?'T':'N';const bubble=document.createElement('div');bubble.className='bubble';bubble.textContent=text;wrap.append(avatar,bubble);chat.append(wrap);chat.scrollTop=chat.scrollHeight;if(persist){history.push({role:role==='user'?'user':'assistant',content:text});history=history.slice(-24);localStorage.setItem(STORE,JSON.stringify(history))}return bubble}for(const item of history){addMessage(item.role,item.content,false)}function setStatus(text,kind=''){statusEl.textContent=text;statusEl.className=`status ${kind}`.trim()}async function refreshStatus(){try{const r=await fetch('/api/status',{cache:'no-store'});const d=await r.json();if(!r.ok)throw new Error(d.error||'status failed');setStatus(`Online · ${d.model}`,'ok');diagnostics.textContent=JSON.stringify(d,null,2)}catch(e){setStatus('Backend nedostupný','error');diagnostics.textContent=String(e)}}async function ask(message){const bubble=addMessage('assistant','Přemýšlím…',false);send.disabled=true;setStatus('Nucleus pracuje…');try{const r=await fetch('/api/chat',{method:'POST',headers:{'content-type':'application/json',...(pin.value?{'x-nucleus-pin':pin.value}:{})},body:JSON.stringify({message,history:history.slice(0,-1)})});const d=await r.json().catch(()=>({}));if(!r.ok)throw new Error(d.error||`HTTP ${r.status}`);bubble.textContent=d.text;history.push({role:'assistant',content:d.text});history=history.slice(-24);localStorage.setItem(STORE,JSON.stringify(history));setStatus(`Online · ${d.model}`,'ok')}catch(e){bubble.textContent=`⚠️ ${e.message}`;setStatus('Chyba AI','error')}finally{send.disabled=false;input.focus()}}form.addEventListener('submit',async e=>{e.preventDefault();const message=input.value.trim();if(!message)return;addMessage('user',message);input.value='';input.style.height='auto';await ask(message)});input.addEventListener('input',()=>{input.style.height='auto';input.style.height=`${Math.min(input.scrollHeight,180)}px`});input.addEventListener('keydown',e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();form.requestSubmit()}});pin.addEventListener('change',()=>localStorage.setItem(PIN_STORE,pin.value));clear.addEventListener('click',()=>{history=[];localStorage.removeItem(STORE);location.reload()});if('serviceWorker'in navigator){navigator.serviceWorker.register('/sw.js').catch(console.error)}refreshStatus();